activityDiagram
    start
    :Продавец: Заполнение формы товара;
    if (Нажата кнопка?) then (Опубликовать)
        :Валидация данных;
        if (Данные корректны?) then (Да)
            :Сохранение в БД (Статус: Модерация);
            :Передача в сервис модерации;
            :Уведомление об успехе;
            stop
        else (Нет)
            :Подсветка ошибок в форме;
            goto :Продавец: Заполнение формы товара;
        endif
    else (Сохранить черновик)
        :Сохранение в БД (Статус: Черновик);
        :Сообщение "Сохранено";
        stop
    endif
